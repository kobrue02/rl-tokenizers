# systems/pretraining/data_prep.py -- sbatch jobs/prep/pretraining_data.sh -c configs/prep/superbpe_culturax.yml
# CPU-only job -- superbpe/bpe need no GPU, same rationale as
# configs/prep/bpe_culturax.yml.
#
# Extends the bpe_culturax/fanta_culturax "large"-scale CulturaX re-run to
# superbpe -- 2026-09-19: glot500 dropped as a pretraining data source
# project-wide, superbpe_50k's own glot500 prep (mid-resume at the time)
# cancelled and removed; this replaces it, not resumes it.
dataset: culturax
langs: all
system: superbpe
checkpoint: checkpoints/superbpe_6351128.pt
output_dir: pretrain_data/culturax_superbpe_large
max_tokens: 13591050230
tokens_per_language: 128217455
lang_batch_size: 5
dedup: false
use_wandb: true
run_name: superbpe_culturax_large_data_prep
