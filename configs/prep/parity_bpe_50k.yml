# systems/pretraining/data_prep.py -- sbatch jobs/prep/pretraining_data.sh -c configs/prep/parity_bpe_50k.yml
dataset: glot500
langs: all
dataset_config: /pfs/work9/workspace/scratch/tu_zxoqp65-rl-tokenizers/data/glot500
system: parity_bpe
# Two parity_bpe tokenizer checkpoints exist on disk (job 6366397 from
# 2026-08-19 14:10, job 6370066 from 2026-08-19 16:47) -- presumably two of
# the three parity_bpe config variants (plain/hybrid/window). This uses the
# later one (6370066); verify against `ls checkpoints/ vocab_out/` on the
# cluster before a real run if that assumption turns out wrong.
checkpoint: checkpoints/parity_bpe_6370066.json
output_dir: pretrain_data/glot500_parity_bpe
max_tokens: 5000000000
use_wandb: true
run_name: parity_bpe_50k_data_prep
